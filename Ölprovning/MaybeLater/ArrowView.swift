//
//  ArrowView.swift
//  Ölprovning
//
//  Created by Petter Gustafsson on 2023-08-31.
//

/*import SwiftUI


struct ArrowView: View {
    @Binding var isVisible: Bool
    @Binding var isAnimating: Bool
    
    var body: some View {
        VStack {
            
            Image(systemName: "arrow.down.circle.fill")
                .resizable()
                .aspectRatio(contentMode: .fit)
                .frame(width: 60, height: 60)
                .offset(y: isAnimating ? -25 : 0) // Move the arrow up
                .animation(
                    Animation.spring(response: 0.6, dampingFraction: 0, blendDuration: 0.1)
                        .repeatForever(autoreverses: true)
                )
            
        }
        .foregroundColor(.orange)
        .opacity(isVisible ? 1 : 0) // Control visibility with opacity
    }
}
*/
